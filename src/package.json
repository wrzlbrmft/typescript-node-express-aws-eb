{
  "name": "typescript-node-express-aws-eb",
  "version": "0.2.0",
  "dependencies": {
    "express": "*"
  },
  "devDependencies": {
    "@types/express": "*",
    "@types/node": "*",
    "typescript": "*"
  },
  "scripts": {
    "prestart": "tsc && node build/prestart.js",
    "start": "node build/start.js",
    "clean": "rm -rf build node_modules"
  }
}
